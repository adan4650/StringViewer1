<Application x:Class="StringViewer1.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            
            <!-- Global Font Settings -->
            <Style TargetType="Window">
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Background" Value="#19183B"/>
                <Setter Property="Foreground" Value="Black"/>
            </Style>
            
            <!-- Global TextBox Styling -->
            <Style TargetType="TextBox">
                <Setter Property="Background" Value="#E7F2EF"/>
                <Setter Property="BorderBrush" Value="White"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Foreground" Value="Black"/>
            </Style>
            
            <!-- Specific styling for file selection ListBox -->
            <Style x:Key="FileSelectionListBoxStyle" TargetType="ListBox">
                <Setter Property="Background" Value="#708993"/>
                <Setter Property="BorderBrush" Value="White"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Foreground" Value="Black"/>
            </Style>

            <!-- Global ListBox Styling (without Foreground) -->
            <Style TargetType="ListBox">
                <Setter Property="Background" Value="#708993"/>
                <Setter Property="BorderBrush" Value="White"/>
                <Setter Property="BorderThickness" Value="1"/>
            </Style>
            
            <!-- Global TextBlock Styling -->
            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="Black"/>
            </Style>
            
            <!-- Global ScrollBar Styling - Fixed -->
            <Style TargetType="ScrollBar">
                <Setter Property="Background" Value="#F0F0F0"/>
                <Setter Property="BorderBrush" Value="#E0E0E0"/>
                <!-- Removed problematic Width setter -->
                <Style.Triggers>
                    <Trigger Property="Orientation" Value="Vertical">
                        <Setter Property="Width" Value="12"/>
                    </Trigger>
                    <Trigger Property="Orientation" Value="Horizontal">
                        <Setter Property="Height" Value="12"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            
            <!-- Global ScrollViewer Styling -->
            <Style TargetType="ScrollViewer">
                <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
                <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>